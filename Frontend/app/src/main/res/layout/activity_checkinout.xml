<?xml version="1.0" encoding="utf-8"?>

<layout>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/appBarLayoutCheckInOut"
            app:layout_constraintTop_toTopOf="parent">

            <com.google.android.material.tabs.TabLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/tabsCheckInOut"
                app:tabTextAppearance="@style/TabStyle"
                app:tabBackground="@color/ColorScheme2"
                app:tabGravity="fill"
                app:tabInlineLabel="true"
                app:tabMaxWidth="0dp"
                app:tabMode="fixed"
                app:tabIndicatorColor="@color/ColorScheme5"
                app:tabSelectedTextColor="@color/ColorScheme5"
                app:tabIconTint="@color/tab_selector_color"/>
<!--                app:tabIconTint="@color/ColorScheme5"-->
<!--                app:tabTextColor="@color/ColorScheme5"/>-->

        </com.google.android.material.appbar.AppBarLayout>

        <androidx.viewpager.widget.ViewPager
            android:id="@+id/viewPagerCheckInOut"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:layout_constraintTop_toBottomOf="@+id/appBarLayoutCheckInOut"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>

